#!/bin/bash

wezterm cli split-pane --top --percent 80
#up=$(wezterm cli get-pane-direction up)
#wezterm cli activate-pane --pane-id $up
#down=$(wezterm cli get-pane-direction down)
#wezterm cli split-pane --right
#wezterm cli activate-pane --pane-id $left
#right=$(wezterm cli get-pane-direction right)
clear

